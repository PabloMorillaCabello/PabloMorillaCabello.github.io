# Server
PORT=3001

# SMTP — Gmail example (use an App Password, not your real password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Where contact emails are delivered
CONTACT_EMAIL=pablomorillacabello@gmail.com

# CORS — restrict to your GitHub Pages domain in production
ALLOWED_ORIGIN=https://pablomorillacabello.github.io
